{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "Debug",
        "preLaunchTask": {
            "type": "shell",
            "command": "cd builddir && ninja"
        },
        "program": "${workspaceFolder}/build/osupp",
        "args": [],
        "cwd": "${workspaceFolder}"
    }
    ]
}